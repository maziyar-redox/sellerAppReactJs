import React from "react";

const Health = () => {
    return (
        <>
            <div className="h-screen flex items-center justify-center">Health</div>
        </>
    );
};

export default Health;